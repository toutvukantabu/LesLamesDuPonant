

<div class="container">
<div class="row formRegister z-depth-5">
{{ form_start(form) }}

{# <img class="responsive-img"src="{{asset('/uploads/discipline/') ~ home_discipline.imageHomeDiscipline }}" alt="" > #}
    {{ form_widget(form) }}
    
    <div class="btnEdit">
    <button class="btn">{{ button_label|default('sauvegarder') }}</button>

