<form method="post" action="{{ path('medieval_family_delete', {'id': medieval_family.id}) }}" onsubmit="return confirm('Confirmer la suppression?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medieval_family.id) }}">
    <button class="btn red">Supprimer</button>
</form>
