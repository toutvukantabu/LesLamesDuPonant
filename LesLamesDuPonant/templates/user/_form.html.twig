{{ form_start(form) }}
<div class="container userEditFull">
	<div class="row formRegister z-depth-5 liste-forum">
		<h4 class="center-align">Edition du profil de
			{% if is_granted('IS_AUTHENTICATED_FULLY') and app.user.pseudoUser != NULL %}
			<span class="name">"{{app.user.pseudoUser}}"</span>
			{% else %}
			<span class=" name">"Illustre inconnu n°{{app.user.id}}"</span>
			{% endif %}</h4>
		<div class="col xl12 l12 m12 s12">
			{% if is_granted('IS_AUTHENTICATED_FULLY') and app.user.photoAvatarUser  != NULL %}
			<div><img class="circle imgUser" src="{{ asset('uploads/avatar/' ~ app.user.photoAvatarUser )}}"></div>
			{% else %}
			<div><img class="circle" src="{{asset('uploads/avatar.jpg')}}"></div>
			{% endif %}</div>
		<div class="col xl12 l12 m12 s12">{{form_row(form.photoAvatarUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.email)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.firstNameUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.lastNameUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.pseudoUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.birthdayDateUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.postalCodeUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.adressUser)}}</div>
		<div class="col xl6 l6 m12 s12">{{form_row(form.cityUser)}}</div>
		<div class="col xl6 l6 m6 s6">
			<div class="btnEdit"><button class="btn">{{ button_label|default('Maj') }}</button></div>
		</div>
		<div class="col xl6 l6 m6 s6">
			<div class="btnEdit"><a class="btn " href="{{path('home')}}"> accueil</a></div>
		</div>
		{{ form_row(form._token) }}