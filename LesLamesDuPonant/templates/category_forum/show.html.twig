{% extends 'base.html.twig' %}

{% block title %}CategoryForum{% endblock %}

{% block body %}
    <h4 class="center-align">CategoryForum</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_forum.id }}</td>
            </tr>
            <tr>
                <th>TitleCategory</th>
                <td>{{ category_forum.titleCategory }}</td>
            </tr>
            <tr>
                <th>DateCategoryForum</th>
                <td>{{ category_forum.dateCategoryForum ? category_forum.dateCategoryForum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_forum_index') }}">retours à la liste</a>

    <a href="{{ path('category_forum_edit', {'id': category_forum.id}) }}">éditer</a>

    {{ include('category_forum/_delete_form.html.twig') }}
{% endblock %}
