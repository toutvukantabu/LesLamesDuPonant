{% extends 'base.html.twig' %}

{% block title %}HomeDiscipline{% endblock %}

{% block body %}
    <h1>HomeDiscipline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ home_discipline.id }}</td>
            </tr>
            <tr>
                <th>NameDiscipline</th>
                <td>{{ home_discipline.nameDiscipline }}</td>
            </tr>
            <tr>
                <th>ImageHomeDiscipline</th>
                <td>{{ home_discipline.imageHomeDiscipline }}</td>
            </tr>
            <tr>
                <th>DescriptionDiscipline</th>
                <td>{{ home_discipline.descriptionDiscipline }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home_discipline_index') }}">back to list</a>

    <a href="{{ path('home_discipline_edit', {'id': home_discipline.id}) }}">edit</a>

    {{ include('home_discipline/_delete_form.html.twig') }}
{% endblock %}
