<form method="post" action="{{ path('home_discipline_delete', {'id': home_discipline.id}) }}" onsubmit="return confirm('Confirmer la suppression?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_discipline.id) }}">
    <button class="btn red">Supprimer</button>
</form>
