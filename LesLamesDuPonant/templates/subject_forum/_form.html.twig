<div class="container">
<div class="row">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btnForm">{{ button_label|default('sauvegarder') }}</button>
{{ form_end(form) }}

</div>
</div>
