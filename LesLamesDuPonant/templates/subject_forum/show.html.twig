{% extends 'base.html.twig' %}

{% block title %}SubjectForum{% endblock %}

{% block body %}
    <h1>SubjectForum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subject_forum.id }}</td>
            </tr>
            <tr>
                <th>TitleSubjectForum</th>
                <td>{{ subject_forum.titleSubjectForum }}</td>
            </tr>
            <tr>
                <th>ContentSubjectForum</th>
                <td>{{ subject_forum.contentSubjectForum }}</td>
            </tr>
            <tr>
                <th>DateSubjectForum</th>
                <td>{{ subject_forum.dateSubjectForum ? subject_forum.dateSubjectForum|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subject_forum_index') }}">back to list</a>

    <a href="{{ path('subject_forum_edit', {'id': subject_forum.id}) }}">edit</a>

    {{ include('subject_forum/_delete_form.html.twig') }}
{% endblock %}
