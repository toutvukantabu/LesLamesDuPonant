{% extends 'default.html.twig' %}

{% block title %}ContactMessage{% endblock %}

{% block body %}
  <H4 class="center-align"> contenu du message</H4>


<div class="container">

<div class="row">




<p>{{ contact_message.dateContactMessage ? contact_message.dateContactMessage|date('Y-m-d H:i:s') : '' }}</p>
<p> {{ contact_message.authorNameContactMessage }}</p>
<p>  {{ contact_message.authorLastNamecContactMessage }}</p>
<p>{{ contact_message.contentContactMessage|raw }}</p>
<p></p>

<a href="{{ contact_message.emailContactMessage }}"><button class="btn">répondre</button></a>


</div>
</div>
                
        
       
               


 

    <a href="{{ path('contact_message_index') }}">retours à la liste</a>

    <a href="{{ path('contact_message_edit', {'id': contact_message.id}) }}">éditer</a>

    {{ include('contact_message/_delete_form.html.twig') }}
{% endblock %}
