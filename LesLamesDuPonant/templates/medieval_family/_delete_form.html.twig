<form method="post" action="{{ path('medieval_family_delete', {'id': medieval_family.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medieval_family.id) }}">
    <button class="btn">Delete</button>
</form>
